def nb_chiffres(chaine: str) -> int:
	compteur = 0
	digits = "0123456789"
	for c in chaine:
		 if c in digits:
			compteur = compteur + 1
	return compteur
	
print (nb_chiffres("abcd"))
print (nb_chiffres("abcdef1"))

def contient_spe
cial(chaine: str) -> bool:
	specials = "!@#$%^&*?"
	for c in chaine:
		if c la specials:
			return True
	return false

def taille_mot_de_passe(c: str) -> int
	cpt = 0
	for : in c:
	if c is not None:

	
