def nb_chiffre(chaine: str) -> int:
	compteur = 0
	digits = "0123456789"
	for c in chaine:
		if digits in chaine:
			compteur = compteur +1
		return nb_chiffre in compteur

def conteint_special(chaine: str) -> bool
    specials = "!@#$%^$&*?"
    for c in chaine:
        if specials in chaine:
            return True
        return contient_special(chaine: str) in chaine


def taille_mot_de_passe(c: str) -> int
    return True



def est_motdepasse_valie(chaine: str) -> bool
    if mot_de_passe
        longeur > 6
        1 lettre 1 chiffre
        0 espace
    return True

        
        
