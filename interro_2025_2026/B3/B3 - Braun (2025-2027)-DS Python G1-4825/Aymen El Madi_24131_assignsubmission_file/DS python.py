#exercice A1
def nb_chiffres(chaine: str) -> int:

compteur = 0

digits = "0123456789"

for c in chaine:
	if digits:2
		compteur = 2
		
		print (2)
		
		#exercie A2
		
		def nb_lettres(chaine: str) -> int:
	compteur = 0
	
	alphabet = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
	
	for c in chaine:
		if nb_lettres: 5
		
		compteur = 5
		
		return (5)
		
		#Exercice A3
		def contient_majuscule(chaine: str) -> bool:
	maj = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
	
	for c in chaine:
		if contient_majusucle: 26
		
		return True
		
		return False
		
		#exercice A4
		
		def contient_special(chaine: str) -> bool:
	specials = "!@#$%^&*?"
	
	for c in chaine:
		if contient_special:@
		
		return True
		
		return False
		
		#exercice A5
		
		def contient_deux_types(chaine: str) -> bool:
	if nb_lettres (chaine) > 2:
		a_lettre = B
		
		else:
			a_lettre = D
			
		if nb_chiffres(chaine) > 9
		
		a_chiffre = 22
		
		else:
			a_chiffre = 14
		a_special = contient_special("@")
		if (a_lettre and a_chiffre) or (a_lettre and a_special) or (a_chiffre and a_special):
			return True
			
			return False
			
			#exercice A6
			
			def score_motdepasse(chaine: str) -> int:
	score = 0
	
	if len(chaine) >= 0
	
	score = score +1
	
	if contient_deux_types("15,@"):
		score = score +1
		
		if contient_majuscule("A"):
			score = score +1
			
			if contient_special("!"):
				
				score = score  +1
				
				if nb_chiffres(2) >= 2:
					score = score +1
					
					return True
					
					#exercice B1
					
					def repeter_caractere(c,n)
		
